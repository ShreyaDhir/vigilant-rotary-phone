prompt('Whatat is your name?');
prompt('What is their name?');

var LoveScore = Math.random() * 100;

if(LoveScore > 70)
{
  alert('Your love score is ' + LoveScore + '% \n' + "You guys are compatible!" );
}
LoveScore  = Math.floor(LoveScore) + 1;
